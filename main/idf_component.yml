description: ESP32 Graphical Bootloader

dependencies:
  m5stack_core_s3:
   version: "^1.0.0"
   rules:
     - if: "target == $BOARD_M5STACK_CORE_S3"
  esp-box-3:
    version: "^1.1.3"
    rules:
     - if: "target == $BOARD_ESP_BOX_3"
  esp32_p4_function_ev_board:
    version: "^1.0.0"
    rules:
     - if: "target == $BOARD_ESP32_P4_FUNCTION_EV_BOARD"
